<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.gurkensalat.android.xingsync" android:versionCode="1" android:versionName="1.0.0-SNAPSHOT">

    <uses-permission
        android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission
        android:name="android.permission.READ_CONTACTS" />
    <uses-permission
        android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission
        android:name="android.permission.READ_SYNC_STATS" />
    <uses-permission
        android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission
        android:name="android.permission.WRITE_SYNC_SETTINGS" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk android:minSdkVersion="11" android:targetSdkVersion="14"/>

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:allowBackup="true" >
        <!-- The authenticator service -->
        <service
            android:name=".authenticator.AuthenticationService"
            android:exported="true">
            <intent-filter>
                <action
                    android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
        <service
            android:name=".syncadapter.SyncService"
            android:exported="true">
            <intent-filter>
                <action
                    android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/syncadapter" />
            <meta-data
                android:name="android.provider.CONTACTS_STRUCTURE"
                android:resource="@xml/contacts" />
        </service>
        <!-- The view notification service -->
        <service
            android:name=".notifier.NotifierService"
            android:exported="true">
            <!--
                No intent-filter here! This activity is only ever launched by
                someone who explicitly knows the class name
            -->
        </service>

        <provider
            android:name=".platform.ContentProviderAdapter"
            android:authorities="com.gurkensalat.android.xingcontacts"
            android:enabled="true"
            android:exported="true"
            android:label="@string/sync_contacts_label"
            android:syncable="true"
            />

        <activity
            android:name=".authenticator.AuthenticatorActivity"
            android:label="@string/authenticator_activity_title"
            android:theme="@android:style/Theme.Dialog"
            android:excludeFromRecents="true"
            android:configChanges="orientation"
            >
            <!--
                No intent-filter here! This activity is only ever launched by
                someone who explicitly knows the class name
            -->
        </activity>

        <activity
            android:name=".authenticator.PrepareRequestTokenActivity"
            android:launchMode="singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="x-oauthflow" android:host="callback" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activites.InviteContactActivity"
            android:theme="@android:style/Theme.Dialog">
            <!--
                We use the INVITE intent to add a raw contact to an existing contact.
                It always comes with a lookup URI.
            -->
            <intent-filter>
                <action
                    android:name="com.android.contacts.action.INVITE_CONTACT" />
                <data
                    android:mimeType="vnd.android.cursor.item/contact" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ViewGroupActivity"
            android:theme="@android:style/Theme.Dialog">
            <!--
                We use the VIEW intent to view a group in our app.
                It always comes with a lookup URI.
            -->
            <intent-filter>
                <action
                    android:name="android.intent.action.VIEW" />
                <data
                    android:mimeType="vnd.android.cursor.item/group" />
            </intent-filter>
        </activity>

        <!-- The Preferences -->
        <activity
            android:name=".preferences.AccountPreferences"
            android:theme="@android:style/Theme.Dialog"
            android:label="Account Preferences"
            android:excludeFromRecents="true" />

        <!-- From old application, to make stuff work... -->
        <!--
        <activity android:name=".HelloAndroidActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->

        <activity android:name=".authenticator.OAuthFlowApp"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
